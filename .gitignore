/target
.claude
PROGRESS.md
.gstack/
bench/server-runs/
bench/results.json
**/__pycache__/
sdks/python/.venv/
sdks/python/uv.lock
.DS_Store
node_modules/
package-lock.json
# The TS SDK needs its lockfile committed (npm ci hard-fails without it).
!sdks/typescript/package-lock.json
sdks/typescript/dist/
sdks/typescript/dist-test/
docker-compose.override.yml
config.local.toml
.env

# Service-account / API-key files (never commit)
bench/secrets/
*-service-account*.json
config.local-bench.toml

# crw bench run artifacts + downloaded datasets (not source; contain model names)
bench/runs/
bench/datasets/

# Internal planning / review docs (keep local, never publish)
plans/
REVIEW-FIXES.md

# ── Secrets / credentials (defense-in-depth; none tracked today) ──
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa*
.netrc

# ── IDE / editor junk ──
.vscode/
.idea/
*.iml
.zed/
.fleet/
*.swp
*.swo
*~
*.orig

# ── OS junk ──
Thumbs.db
Desktop.ini

# ── Logs / temp / caches ──
*.log
logs/
*.tmp
.cache/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.py[cod]
*.profraw

# ── Internal design/planning notes (keep local) ──
futures-plans.md
