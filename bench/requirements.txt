# Benchmark harness deps. Pin versions so a run is reproducible (numpy is
# hashed into every manifest). Set up with:
#   uv venv bench/.venv && uv pip install --python bench/.venv/bin/python -r bench/requirements.txt
numpy==2.5.1
aiohttp==3.14.1
datasets==5.0.0
